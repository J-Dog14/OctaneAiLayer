# Postgres driver (binary build = no compiler needed)
psycopg2-binary>=2.9.9

# Google AI Studio / Gemini SDK
google-generativeai>=0.8.0

# .env loader
python-dotenv>=1.0.1

# CLI framework
click>=8.1.0

# Pretty terminal output (optional but nice for debugging)
rich>=13.7.0
